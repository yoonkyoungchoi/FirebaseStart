<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="kr.hs.emirim.choi.firebaseStart">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".storage.MetaInfoActivity"></activity>
        <activity android:name=".activity_storage" />
        <activity android:name=".Activity_Firestory" />
        <activity android:name=".realtimedb.MemoActivity" />
        <activity android:name=".SignedInActivity" />
        <activity android:name=".FirebaseUIActivity" />
        <activity android:name=".AuthActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".firestore.FirestoreActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>